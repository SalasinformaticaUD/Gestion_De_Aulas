import { Body, Controller, Get, Param, Patch, Post, Query } from '@nestjs/common'; import { RequireModule } from '../auth/decorators/require-module.decorator'; import { RequirePermissions } from '../auth/decorators/require-permissions.decorator'; import { CurrentUser } from '../auth/decorators/current-user.decorator'; import type { UsuarioAutenticado } from '../auth/auth.types'; import { EstudiantesService } from './estudiantes.service'; import { CreateEstudianteDto } from './dto/create-estudiante.dto'; import { UpdateEstudianteDto } from './dto/update-estudiante.dto';
@RequireModule('ADMINISTRACION') @Controller('estudiantes') export class EstudiantesController { constructor(private readonly service: EstudiantesService) {} @Post() @RequirePermissions('ADMINISTRACION_CREAR') create(@Body() dto: CreateEstudianteDto, @CurrentUser() user?: UsuarioAutenticado) { return this.service.create(dto, user?.id); } @Get() @RequirePermissions('ADMINISTRACION_LEER') findAll(@Query('q') q?: string) { return this.service.findAll(q); } @Get(':id') @RequirePermissions('ADMINISTRACION_LEER') one(@Param('id') id: string) { return this.service.findOne(id); } @Patch(':id') @RequirePermissions('ADMINISTRACION_ACTUALIZAR') update(@Param('id') id: string, @Body() dto: UpdateEstudianteDto, @CurrentUser() user?: UsuarioAutenticado) { return this.service.update(id, dto, user?.id); } }
