"use client";
import { useState,type FormEvent } from "react";
import estilos from "./SistemaVisualMonitores.module.css";
type Tipo="memorandos"|"actas";type Documento={id:string;numero:string;monitor:string;fecha:string;asunto:string;estado:string};
export function GestionDocumental({tipo}:{tipo:Tipo}){const esMemorando=tipo==="memorandos";const[titulo,setTitulo]=useState("");const[monitor,setMonitor]=useState("Kaleth Molina");const[fecha,setFecha]=useState(new Date().toISOString().slice(0,10));const[documentos,setDocumentos]=useState<Documento[]>([{id:"1",numero:esMemorando?"MEM-2026-014":"ACT-2026-008",monitor:"Kaleth Molina",fecha:"2026-08-20",asunto:esMemorando?"Seguimiento a retrasos registrados":"Reunión de seguimiento mensual",estado:"Vigente"}]);const guardar=(e:FormEvent)=>{e.preventDefault();setDocumentos(actual=>[{id:String(Date.now()),numero:`${esMemorando?"MEM":"ACT"}-2026-${String(actual.length+15).padStart(3,"0")}`,monitor,fecha,asunto:titulo,estado:"Vigente"},...actual]);setTitulo("")};const nombre=esMemorando?"Memorandos":"Actas";return <><section className={`page-heading ${estilos.encabezado}`}><div><span className={estilos.etiqueta}>Gestión documental</span><h1>{nombre}</h1><p>{esMemorando?"Registre comunicaciones de seguimiento, llamados de atención y compromisos del monitor.":"Consolide reuniones, acuerdos y responsables del proceso de monitorías."}</p></div></section><div className={estilos.distribucionFormulario}><section className={`${estilos.tarjeta} ${estilos.formularioFijo}`}><header><div><h2>Nuevo {esMemorando?"memorando":"acta"}</h2><p>El consecutivo se genera automáticamente en modo demo.</p></div></header><form className={estilos.formulario} onSubmit={guardar}><label className={estilos.campo}><span>Monitor relacionado</span><select value={monitor} onChange={e=>setMonitor(e.target.value)}><option>Kaleth Molina</option><option>Sandra Ramírez</option><option>Iván Prado</option></select></label><label className={estilos.campo}><span>Fecha</span><input type="date" value={fecha} onChange={e=>setFecha(e.target.value)}/></label><label className={estilos.campo}><span>{esMemorando?"Motivo y compromiso":"Tema y acuerdos"}</span><textarea value={titulo} onChange={e=>setTitulo(e.target.value)} required placeholder={esMemorando?"Describa el motivo, compromisos y plazo...":"Describa los acuerdos y responsables..."}/></label><button className="button-primary">Registrar {esMemorando?"memorando":"acta"}</button></form></section><section className={estilos.tarjeta}><header><div><h2>{nombre} registrados</h2><p>Consulte, edite o descargue el soporte documental.</p></div></header><div className={estilos.tablaContenedor}><table className={estilos.tabla}><thead><tr><th>Número</th><th>Monitor</th><th>Fecha</th><th>Descripción</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>{documentos.map(item=><tr key={item.id}><td><strong>{item.numero}</strong></td><td>{item.monitor}</td><td>{item.fecha}</td><td>{item.asunto}</td><td><span className={`${estilos.insignia} ${estilos.exito}`}>{item.estado}</span></td><td><div className={estilos.accionesTabla}><button>Ver</button><button>Editar</button><button>Descargar</button></div></td></tr>)}</tbody></table></div></section></div></>}
