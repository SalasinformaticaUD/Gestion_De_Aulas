"use client";

import { useEffect, useMemo, useState, type FormEvent } from "react";
import { servicioMonitores } from "@/features/monitores/api/servicioMonitores";
import type { HorarioApi, MonitorApi } from "@/features/monitores/api/contratosMonitores";
import estilos from "./SistemaVisualMonitores.module.css";

const dias = ["Lunes", "Martes", "Miércoles", "Jueves", "Viernes", "Sábado", "Domingo"];
const vacio = { monitor: "", weekday: 0, start_time: "08:00:00", end_time: "12:00:00", location: "", is_active: true };

export function HorariosApiView() {
  const [rows, setRows] = useState<HorarioApi[]>([]); const [monitores, setMonitores] = useState<MonitorApi[]>([]); const [form, setForm] = useState(vacio); const [buscar, setBuscar] = useState(""); const [estado, setEstado] = useState("TODOS"); const [error, setError] = useState(""); const [aviso, setAviso] = useState(""); const [cargando, setCargando] = useState(true); const [guardando, setGuardando] = useState(false);
  async function cargar() { setCargando(true); setError(""); try { const [horarios, monitoresApi] = await Promise.all([servicioMonitores.listarHorarios(), servicioMonitores.listarMonitores()]); setRows(horarios); setMonitores(monitoresApi); setForm((actual) => ({ ...actual, monitor: actual.monitor || monitoresApi[0]?.id || "" })); } catch (e) { setError(e instanceof Error ? e.message : "No fue posible cargar los horarios."); } finally { setCargando(false); } }
  useEffect(() => { void cargar(); }, []);
  const visibles = useMemo(() => rows.filter((item) => { const texto = `${item.monitor_name} ${item.location}`.toLocaleLowerCase("es"); return (!buscar || texto.includes(buscar.toLocaleLowerCase("es"))) && (estado === "TODOS" || (estado === "ACTIVOS" ? item.is_active : !item.is_active)); }), [rows, buscar, estado]);
  async function crear(evento: FormEvent<HTMLFormElement>) { evento.preventDefault(); if (!form.monitor) { setError("Registre primero un monitor para poder asignar un horario."); return; } setGuardando(true); setError(""); setAviso(""); try { const creado = await servicioMonitores.crearHorario(form); setRows((actual) => [creado, ...actual]); setForm({ ...vacio, monitor: form.monitor }); setAviso("Horario registrado correctamente en la API."); } catch (e) { setError(e instanceof Error ? e.message : "No fue posible registrar el horario."); } finally { setGuardando(false); } }
  async function alternar(item: HorarioApi) { try { const actualizado = await servicioMonitores.actualizarHorario(item.id, { is_active: !item.is_active }); setRows((actual) => actual.map((row) => row.id === item.id ? actualizado : row)); } catch (e) { setError(e instanceof Error ? e.message : "No fue posible actualizar el horario."); } }
  async function eliminar(item: HorarioApi) { if (!window.confirm(`¿Eliminar el horario de ${item.monitor_name}?`)) return; try { await servicioMonitores.eliminarHorario(item.id); setRows((actual) => actual.filter((row) => row.id !== item.id)); setAviso("Horario eliminado correctamente."); } catch (e) { setError(e instanceof Error ? e.message : "No fue posible eliminar el horario."); } }
  return <div className={estilos.gestionCuentas}><section className={`page-heading ${estilos.encabezado}`}><div><span className={estilos.etiqueta}>Gestión académica</span><h1>Horarios</h1><p>Asigne turnos reales a los monitores y consulte los registros guardados por la API.</p></div><button type="button" className={estilos.botonSecundario} onClick={() => void cargar()} disabled={cargando}>Actualizar</button></section><section className={estilos.metricas}><article className={`${estilos.metrica} ${estilos.violeta}`}><span>Total</span><strong>{rows.length}</strong><small>Horarios registrados</small></article><article className={`${estilos.metrica} ${estilos.verde}`}><span>Activos</span><strong>{rows.filter((item) => item.is_active).length}</strong><small>Turnos vigentes</small></article><article className={`${estilos.metrica} ${estilos.ambar}`}><span>Inactivos</span><strong>{rows.filter((item) => !item.is_active).length}</strong><small>Turnos suspendidos</small></article><article className={`${estilos.metrica} ${estilos.azul}`}><span>Visibles</span><strong>{visibles.length}</strong><small>Según filtros actuales</small></article></section>{error && <div className={`${estilos.aviso} ${estilos.avisoError}`} role="alert">{error}</div>}{aviso && <div className={`${estilos.aviso} ${estilos.avisoExito}`} role="status">{aviso}</div>}<div className={estilos.distribucionFormulario}><section className={`${estilos.tarjeta} ${estilos.formularioFijo}`}><header><div><h2>Asignar horario</h2><p>Complete todos los datos del turno.</p></div></header><form className={estilos.formulario} onSubmit={crear}><label className={estilos.campo}><span>Monitor</span><select required value={form.monitor} onChange={(e) => setForm({ ...form, monitor: e.target.value })}><option value="">Seleccione un monitor</option>{monitores.map((item) => <option key={item.id} value={item.id}>{item.full_name}</option>)}</select></label><label className={estilos.campo}><span>Día</span><select value={form.weekday} onChange={(e) => setForm({ ...form, weekday: Number(e.target.value) })}>{dias.map((dia, index) => <option key={dia} value={index}>{dia}</option>)}</select></label><div className={estilos.formularioDoble}><label className={estilos.campo}><span>Hora inicial</span><input required type="time" value={form.start_time.slice(0, 5)} onChange={(e) => setForm({ ...form, start_time: `${e.target.value}:00` })} /></label><label className={estilos.campo}><span>Hora final</span><input required type="time" value={form.end_time.slice(0, 5)} onChange={(e) => setForm({ ...form, end_time: `${e.target.value}:00` })} /></label></div><label className={estilos.campo}><span>Ubicación</span><input required value={form.location} onChange={(e) => setForm({ ...form, location: e.target.value })} placeholder="Aula, laboratorio o sede" /></label><label className={estilos.verificacion}><input type="checkbox" checked={form.is_active} onChange={(e) => setForm({ ...form, is_active: e.target.checked })} /><span><strong>Horario activo</strong><br />Disponible para el cálculo de asistencia.</span></label><button className="button-primary" disabled={guardando || !monitores.length}>{guardando ? "Guardando…" : "Asignar horario"}</button></form></section><section className={estilos.tarjeta}><header><div><h2>Horarios registrados</h2><p>La tabla se muestra vacía cuando la API aún no tiene datos.</p></div><span className={`${estilos.insignia} ${estilos.neutro}`}>{visibles.length} resultados</span></header><div className={estilos.barraHerramientas}><label className={estilos.campoAncho}><span>Buscar</span><input value={buscar} onChange={(e) => setBuscar(e.target.value)} placeholder="Monitor o ubicación" /></label><label className={estilos.campo}><span>Estado</span><select value={estado} onChange={(e) => setEstado(e.target.value)}><option value="TODOS">Todos</option><option value="ACTIVOS">Activos</option><option value="INACTIVOS">Inactivos</option></select></label></div><div className={estilos.tablaContenedor}><table className={estilos.tabla}><thead><tr><th>Monitor</th><th>Día</th><th>Horario</th><th>Ubicación</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>{cargando ? <tr><td colSpan={6} className={estilos.vacio}>Cargando horarios…</td></tr> : visibles.map((item) => <tr key={item.id}><td><strong>{item.monitor_name || "Sin nombre"}</strong></td><td>{dias[item.weekday] || item.weekday}</td><td>{item.start_time.slice(0, 5)} – {item.end_time.slice(0, 5)}</td><td>{item.location || "Sin ubicación"}</td><td><span className={`${estilos.insignia} ${item.is_active ? estilos.exito : estilos.neutro}`}>{item.is_active ? "Activo" : "Inactivo"}</span></td><td><div className={estilos.accionesTabla}><button type="button" onClick={() => void alternar(item)}>{item.is_active ? "Desactivar" : "Activar"}</button><button type="button" className={estilos.eliminar} onClick={() => void eliminar(item)}>Eliminar</button></div></td></tr>)}{!cargando && !visibles.length && <tr><td colSpan={6} className={estilos.vacio}>No hay horarios registrados o coincidentes.</td></tr>}</tbody></table></div></section></div></div>;
}
