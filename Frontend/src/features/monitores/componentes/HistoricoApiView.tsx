"use client";
import { useEffect, useState } from "react";
import { servicioMonitores } from "@/features/monitores/api/servicioMonitores";
type Fila = Record<string, unknown>;
export function HistoricoApiView() { const [rows, setRows] = useState<Fila[]>([]); const [error, setError] = useState(""); useEffect(() => { void servicioMonitores.listarHistorico().then((data) => setRows(data as Fila[])).catch((e) => setError(e instanceof Error ? e.message : "No fue posible cargar el histórico.")); }, []); return <><section className="page-heading"><div><span>Trazabilidad</span><h1>Histórico</h1><p>Resumen histórico de monitores y horas por semestre, desde la API.</p></div></section>{error && <div role="alert">{error}</div>}<section className="card"><table><thead><tr><th>Semestre</th><th>Monitor</th><th>Dependencia</th><th>Normales</th><th>Extra aprobadas</th><th>Total</th><th>Restantes</th></tr></thead><tbody>{rows.map((row) => <tr key={String(row.monitor_id)}><td>{String(row.semester)}</td><td>{String(row.monitor_name)}</td><td>{String(row.department)}</td><td>{String(row.normal_hours)} h</td><td>{String(row.approved_overtime_hours)} h</td><td>{String(row.total_hours)} h</td><td>{String(row.remaining_hours)} h</td></tr>)}</tbody></table></section></>; }
